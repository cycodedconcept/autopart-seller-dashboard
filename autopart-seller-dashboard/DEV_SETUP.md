Hello samuel,

You have been added to the autoparts-seller-dashboard repository.

Please accept the GitHub invite, then run:

git clone https://github.com/cycodedconcept/autopart-seller-dashboard.git

cd autoparts-seller-dashboard

git checkout dev/seller-dashboard

npm install

npm run dev

When you make changes, push like this:

git add .
git commit -m "Describe work done"
git push origin dev/seller-dashboard

Please do not push directly to main. Push only to dev/seller-dashboard.