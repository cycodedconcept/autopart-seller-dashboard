Sales and payouts verification — 12 September 2026

The sales implementation is present at `/sales`, with payout details at `/sales/payouts/:id` and legacy `/transactions` links retained. The six test failures found during the status check are resolved. **174 tests pass across 23 files**, the production build passes, and ESLint passes for the sales implementation and affected tests/form. Live acceptance remains blocked by the documented development seller account being suspended.

**Existing implementation verified**

- `src/services/sales.js` uses the existing Axios client for `GET /seller/sales`, `GET /seller/payouts`, and `POST /seller/payouts`. Services unwrap the success envelope, date boundaries remain date-only strings, payout history uses server status/page/limit parameters, and a payout request sends only the trimmed `bankAccountRef`.
- `src/features/transactionSlice.js` follows the established Redux Toolkit thunk pattern, keeps sales/history/detail/request state separate, ignores stale list responses, prevents concurrent payout requests, and refreshes the selected queries after success.
- `src/pages/Transactions.jsx` composes `SalesOverview`, `SalesDateFilter`, `SalesLoading`, `PayoutHistory`, `PayoutRequestForm`, and `PayoutStatusBadge` from `src/components/sales/`. It displays amounts through the existing kobo/naira helpers, keeps all-date payout balances distinct from the selected sales period, and includes loading, empty, validation and retry states.
- `src/pages/transactions/TransactionDetail.jsx` supports direct links, real payout amounts/status/dates and optional approval/rejection metadata. There is no seller payout-by-ID endpoint in the local backend; direct links search unfiltered, paginated history. Print and PDF controls already exist; unit tests exercise print dispatch and PDF failure handling.
- `src/utils/sales.js`, `src/types/sales.js`, and `src/styles/sales.css` provide validation/date formatting, PropTypes, and responsive styling. Routes and navigation are already connected through `src/App.jsx` and `src/components/Layout.jsx`.

The API origin remains configured in `src/config/constant.js`; development uses the existing `/api` Vite proxy. `src/utils/axios.js` attaches the existing localStorage bearer token, normalizes errors and clears authentication on a protected 401. No second client, dependencies, token mechanism, refresh mechanism or retry mechanism was added.

The local backend checkout at `/Users/mac/Desktop/autopart-backend` was inspected read-only. Its `src/routes/seller-finance.routes.js`, `src/validators/seller-finance.validator.js`, and `src/services/seller-finance.service.js` confirm the three finance routes, paired date filters, bank-reference length of 3–255 characters, maximum payout page size of 50, full payout history rows and 409 response when there are no eligible completed sales. These findings describe the checkout, not independently verified deployed behavior.

**Every project file changed in this follow-up**

| File | Change and reason |
| --- | --- |
| `src/components/products/ProductForm.jsx` | Fixed the condition dropdown. It previously excluded the existing uppercase `OEM` value from its fallback while rendering an unsupported lowercase `oem` option, causing an OEM product to display as New. Create now offers the required New/Used choices. Edit retains the original condition option, including when the seller changes away and then back. |
| `tests/components/products/ProductForm.test.jsx` | Retained the OEM prefill/submission regression test and added two cases: create offers only new/used; an existing OEM product can switch to used and back before submitting uppercase OEM. Now 13 cases. |
| `tests/components/sales/PayoutRequestForm.test.jsx` | Replaced 256 individually scheduled keystrokes with one input change for the overlength boundary. The timeout had allowed typing to continue into later tests. Added explicit zero-POST assertions for invalid input. Replaced the 100 ms response race with a controlled promise, then verified the pending controls, exact JSON payload and duplicate-click prevention before releasing the response. |
| `tests/pages/Transactions.test.jsx` | Scoped payout status assertions to table cells. The old text query also matched the Requested date-column heading. |
| `tests/pages/transactions/TransactionDetail.test.jsx` | Scoped the Requested assertion to the badge instead of also matching the requested-date label. |
| `SALES_MILESTONE.md` | Created this report. |

No files were rewritten in full in this follow-up. The existing sales UI/services were usable; the failures required targeted assertion/timing fixes and the product dropdown correction. No backend, buyer or admin code was modified. No dependencies were added. This workspace has no `.git` directory, so no commit was created. Build output in `dist/` was regenerated by the existing build command.

**Tests and checks**

The existing six sales test files now pass all **56 cases**:

| Test file | Cases | Coverage |
| --- | ---: | --- |
| `tests/services/sales.test.js` | 14 | Methods, URLs, bearer auth, envelopes, date-only queries, status/pagination, exact payout JSON, validation/API errors and paginated direct-detail lookup. |
| `tests/utils/sales.test.js` | 11 | Invalid/paired/reversed dates, bank-reference boundaries, date formatting and missing dates. |
| `tests/features/transactionSlice.test.js` | 7 | Independent state/errors, stale requests, duplicate writes, failure retention, current-query refresh and cached newly created payout detail. |
| `tests/pages/Transactions.test.jsx` | 7 | Accurate summary/balances, zero values, validation, API filters/pagination, independent failures/retry and detail links. |
| `tests/components/sales/PayoutRequestForm.test.jsx` | 10 | Validation, review/cancel, in-flight protection, exact result/refresh, field errors, no-eligible-sales conflict, refresh failure and disabled requests. |
| `tests/pages/transactions/TransactionDetail.test.jsx` | 7 | Direct/legacy links, real financial/status/date metadata, missing/error/retry states, print dispatch and PDF failure feedback. |

All automated network calls are mocked with the existing MSW setup. `npm test` passes 174/174 across 23 files, including the two new product regressions. The targeted affected-file run passes 37/37. `npm run build` passes with the existing large-chunk warning. Scoped ESLint passes; whole-repository lint was not rerun for unrelated files.

An isolated headless Chrome profile verified summary rendering, date/status filters, server pagination, review and cancellation without a POST, one confirmed mocked payout, list/balance refresh, detail navigation and hard reload, and independent sales error/retry. The successful run made **21 mocked API requests**, including one mocked payout POST, with **zero JavaScript exceptions**. Desktop (1440 px) and mobile (390 px) summary/detail screens had no document-level horizontal overflow. Screenshots were inspected visually. No live payout was created.

Browser artifacts and request results are in `/private/tmp/sales-browser-dRiVE5/`; the temporary verification script is `/private/tmp/sales-browser-check.mjs`. Pre-edit copies of all five edited source/test files are in `/private/tmp/sales-fixes-before-ld1907_a/`. These temporary artifacts are not project dependencies or committed tests.

**Remaining live verification**

An actual login attempt against the already configured API using the development credentials documented in the project returned **HTTP 403**, `success: false`, and **“This account has been suspended.”** No token was obtained. Credentials were not copied into new files or tool output.

An active test seller account and its valid payout bank reference are needed to verify deployed sales responses, history queries and a real payout request through the UI. The live account prerequisite is the outstanding blocker; passing mocked tests does not establish live end-to-end acceptance. The PDF success path was not manually exercised in Chrome.
